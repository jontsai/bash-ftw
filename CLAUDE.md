# CLAUDE.md

Read `AGENTS.md` for project guidelines, file structure, and contribution rules.

## Quick Reference

- **Add install recipes** → `dotfiles/.bashrc.ftw` (Installation Cheatsheets section)
- **Add aliases/functions** → `dotfiles/.bashrc.ftw` (appropriate section)
- **Test changes** → `make install && rebash`
- **Avoid `bin/`** unless script must run before BASH loads
